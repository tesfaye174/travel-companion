<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingHorizontal="32dp"
    android:paddingVertical="48dp">

    <!-- Icon Circle -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="120dp"
        android:layout_height="120dp"
        app:cardBackgroundColor="?attr/colorPrimaryContainer"
        app:cardCornerRadius="60dp"
        app:cardElevation="0dp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center">

            <ImageView
                android:id="@+id/iv_empty_icon"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_gravity="center"
                android:contentDescription="@string/no_trips_yet"
                android:src="@drawable/ic_empty_trips"
                app:tint="?attr/colorPrimary" />

        </FrameLayout>

    </com.google.android.material.card.MaterialCardView>

    <!-- Title -->
    <TextView
        android:id="@+id/tv_empty_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:text="@string/no_trips_yet"
        android:textAppearance="@style/TextAppearance.TravelCompanion.TitleLarge"
        android:textColor="?attr/colorOnSurface"
        tools:text="No trips yet" />

    <!-- Message -->
    <TextView
        android:id="@+id/tv_empty_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="@string/no_trips_yet_message"
        android:textAppearance="@style/TextAppearance.TravelCompanion.BodyMedium"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:lineSpacingMultiplier="1.4"
        tools:text="Start your adventure by creating your first trip" />

    <!-- Primary Action -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_empty_action"
        style="@style/Widget.TravelCompanion.Button.Primary"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:layout_marginTop="24dp"
        android:text="@string/create_trip"
        android:textAllCaps="false"
        app:cornerRadius="24dp"
        app:icon="@drawable/ic_add"
        app:iconGravity="textStart"
        app:iconPadding="8dp" />

    <!-- Secondary Action -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_empty_explore"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/explore"
        android:textAllCaps="false"
        app:icon="@drawable/ic_map"
        app:iconGravity="textStart"
        app:iconPadding="8dp" />

</LinearLayout>
