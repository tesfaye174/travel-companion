<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Colore quando selezionato (bianco pieno) -->
    <item android:color="@android:color/white" android:state_checked="true" />
    <!-- Colore quando non selezionato (bianco con trasparenza) -->
    <item android:color="@color/navBarInactive" android:state_checked="false" />
</selector>
